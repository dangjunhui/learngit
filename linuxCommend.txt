# linux 命令 -- 查询文件中是否包含某个字符
grep --color=auto -rns "hello world" *

# grep:Globally search a Regular Expression and Print. 用于过滤/搜索的特定字符。可使用正则表达式，能匹配多种命令配合使用
# * 表示当前目录所有文件，也可以写具体的目录名或文件名
# -r 递归查找 -R与此相同
# -n 查询结果现实行号
# -i 忽略大小写
# -s 不显示报错信息
# --color=auto 可以将查询的字符串使用不同的颜色显示
